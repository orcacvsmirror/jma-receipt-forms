【ソフト名】  (預り金)患者一覧表 プログラム(A00000L107) 
【バージョン】V02.50(20050301)
              V02.60(20060809)
              V02.70(20070918)
              V02.80(20080403)
              V02.81(20090206)
              V02.82(20090326)
              V02.83(20090512,20090513)

【動作環境】  日医標準レセプトソフトが動作するLinuxマシン

変更点 sarge対応                                2006/08/09
       Ver4.0対応                               2007/09/18
       install.shにEtchで使用するコード
       UTF-8への変換を追加                      2008/04/03
       シス管の値が取得できない不具合を修正     2008/04/03
       H20年4月改正対応
       詳細（記号・番号１６文字以上対応）
           （後期高齢者対応）                   2008/04/03
       install.shをCVSファイル構造に対応            2009/02/06
       install.shをINIファイル上書き確認仕様に対応  2009/02/06
       バージョン表記対応                       2009/03/26
       install.shをバージョン表記に対応         2009/03/26
       install.shでINIファイルのコピー処理を削除(ver4.4.0対応)  2009/05/12
       プログラムオプション対応(INIファイル)    2009/05/13

1.はじめに

  このプログラムは指定日／指定期間において、預り金の患者の一覧を印字します。
  このプログラムは単体では動作しません。患者一覧表起動プログラムと連動して動作します。
  
  日医標準レセプトソフト(以下、日レセ)には標準で預り金は取り扱っていませんので
  預り金の取り扱いには預り金用の自費コードを用意して頂く必要があります。
  預り金用自費コードの用意、取り扱いに関しましては項目３「預り金の設定について」にて説明致します。


2.インストール方法について

  このプログラムを次の手順でインストールします。

  ・ソースのコピーとコンパイル
  ・システムの再起動
 
 
   1)ソースのコピーとコンパイル
       まず、./A00000L107/のディレクトリに移動してください。
	   次のインストールシェルをrootユーザで実行してください。
       
	   bash install.sh

	   実行するとソースのコピーを行った後にコンパイルを自動で行います。

 
   2)システムの再起動
       コンパイル作業終了後に必ずシステムの再起動(リブート)を実施して下さい。

					   
 
   (注１) 日医標準レセプトソフト標準パッケージ実行環境に対するモジュールコピーは必要ありません。
   (注２) /usr/lib/jma-receipt/lddef/orcabt.bdファイルにプログラム情報を追加する必要はありません。


3.預り金の設定について

  3.1.預り金用 項目割り当て
    預り金については日レセに用意されていないため「点数マスタの自費コード」を１項目"預り金"として割り当て使用します。

	1)自費名称の設定
	  「01 医事業務」-「91 マスタ登録」-「101 システム管理マスタ」-「1013 請求書自費名称」を選択。
	  区分コードは任意の値を設定する。この値は2)でも使用します。
	  有効期間は 00000000〜99999999
      自費名称を「預り金」として登録を行ってください。

	2)預り金用自費コードの作成
	  「01 医事業務」-「91 マスタ登録」-「102 点数マスタ」を選択。
      診療行為コードは095から始まる任意の９桁コードを入力してください。
	  例：095000001
	  コードが入力できましたら、確定を押し画面を切替えてください。

	   有効年月日「00000000〜99999999」
	   カナ名称  「アズカリキン」
	   漢字名称  「預り金」
	   正式名称  「預り金」

       カラム位置、桁数はすべて0
	   金額      「0」
	   保険適用  「2 保険適用外」
	   点数欄集計先識別(外来)   1)で設定した区分コードを入力してください。
	   点数欄集計先識別(入院)   1)で設定した区分コードを入力してください。
       部位区分  「0 その他」

       入力チェック区分 「0」

      最後に登録を行ってください。

    3)患者一覧帳票への預り金用自費コードの登録
	  2)で登録した預り金用自費コードを「91 マスタ登録」「101 システム管理マスタ」
	  「1910 プログラムオプション情報」へ設定し登録してください。
